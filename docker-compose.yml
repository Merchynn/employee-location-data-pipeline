services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: employee_locations
      POSTGRES_USER: employee
      POSTGRES_PASSWORD: employee
    ports:
      - "5432:5432"
    volumes:
      - employee_location_postgres:/var/lib/postgresql/data

volumes:
  employee_location_postgres:
